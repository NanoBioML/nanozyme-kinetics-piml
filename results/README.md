# Frozen analysis outputs

Upload the summary files produced by the **final frozen run** of `code/PIML_revision_grouped_oof.py` here.

Expected manuscript summary files:

```text
Publication_Grouped_OOF_Summary.csv
Random_vs_Publication_Grouped_FullModel.csv
CV_Paired_R2_Deltas_Summary.csv
KnownActivityOnly_A0_A3_CV_Summary.csv
```

For a complete archival run, the full generated output directory may additionally be stored under:

```text
results/full_run/
```

Do not mix outputs from earlier script versions with the frozen manuscript run. The final run should correspond to the DOI-annotated 86-record dataset in `data/Database_86_with_DOI.xlsx`.
